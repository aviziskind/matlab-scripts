function c = color_s(color_index)
    allcolors = 'bgrcmyk';
    color_index = mod(color_index - 1, length(allcolors)) + 1;
    c = allcolors(color_index);
end

%{
 colors in rgb:

    b [0 0 1]
    g [0 1 0]
    r [1 0 0]
    c [0 1 1]
    m [1 0 1]
    y [1 1 0]
    k [0 0 0]

%}

